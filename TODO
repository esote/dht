=== TODO ===

rtable - check NodeID sizes
kbucket - check NodeID sizes
storer - check Key sizes

maybe keep full key 64 bytes when sending requests, but only use last 32 for LCP
XOR metric.

Review papers listed in PAPERS.

Unit tests & benchmarks.

Add README

Rewrite PROTOCOL using nroff, which can convert to an IETF-style document for
easier reading.
